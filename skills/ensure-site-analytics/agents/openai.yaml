interface:
  display_name: "Ensure Site Analytics"
  short_description: "Require and verify analytics on every site"
  default_prompt: "Use $ensure-site-analytics to verify mandatory runtime analytics, URL reporting, Search Console, and infrastructure evidence, then repair defects through $deliver-github-pr with production verification."
