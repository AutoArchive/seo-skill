interface:
  display_name: "Change SEO Site"
  short_description: "Ship verified incremental SEO changes through pull requests"
  default_prompt: "Use $change-seo-site to ship one evidence-backed, incremental, reversible improvement through a real pull request. Preserve unrelated production behavior, state the blast radius and rollback, wait for CI, self-review, squash merge, verify the exact production deployment and representative unaffected routes, and merge the closeout pull request."
